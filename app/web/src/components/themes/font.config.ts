// ============================================================
// Font configuration
// ============================================================
// The Next version generated CSS-variable classNames here with
// next/font/google and applied them to <body>. In this Vite app the families
// are loaded by the <link> in index.html and the matching CSS variables are
// declared in src/styles/fonts.css.
//
// `fontVariables` is kept as an export so every consumer compiles unchanged;
// it is now empty because the variables are global rather than class-scoped.
// To add a family: add it to the <link> in index.html AND declare its
// --font-* variable in src/styles/fonts.css.
// ============================================================

export const fontVariables = '';
